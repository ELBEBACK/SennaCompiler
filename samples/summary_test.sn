func add(a, b) {
    c = a + b;
    c += 1;
    add(c, b);
    while( c > 2) {
        if(a == 1) {
            break;
        }
    }
}

for(e = 0; e < 1; e++) {
    c = add(1, 2);
}

res = add(10, 20);

print res;
